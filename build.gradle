plugins {
    id 'java'
    id 'application'
}

mainClassName = 'App'

dependencies {

    compile 'org.springframework.boot:spring-boot-starter-web:2.0.4.RELEASE'

    testCompile 'junit:junit:4.12'
}

repositories {
    jcenter()
}
